# Run with `dart run ffigen --config ffigen.yaml`.
name: SelectionHookFlutterBindings
description: |
  Bindings for `src/bridge/c_api.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/selection_hook_flutter_bindings_generated.dart'
headers:
  entry-points:
    - 'src/bridge/c_api.h'
  include-directives:
    - 'src/bridge/c_api.h'
comments:
  style: any
  length: full
